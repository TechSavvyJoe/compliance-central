{
  "name": "compliance-central",
  "version": "1.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for f in $(find . -name '*.js' -not -path './lib/jspdf.umd.min.js' -not -path './docs/lib/zxing.min.js' -not -path './docs/lib/zxing-wasm/*' -not -path './lib/qrcode.min.js' -not -path './node_modules/*'); do node --check \"$f\" || exit 1; done",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "node --test",
    "test:browser": "node tools/verify-panel-flows.mjs && node tools/verify-extension-flows.mjs && node --test tools/browser/scanner-flows.mjs",
    "assets": "node tools/capture-store-shots.mjs",
    "package": "bash tools/package-extension.sh"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "puppeteer-core": "25.10.0"
  }
}
