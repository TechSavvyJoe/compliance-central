PRIVACY TAB — copy each block into the matching field
(Dashboard: left sidebar → Privacy)


==================================================
SINGLE PURPOSE  (one text box)
==================================================
Compliance Central lets Michigan automotive dealers screen buyers and co-buyers for OFAC sanctions, MDOS repeat-offender status, and vehicle title/lien status, calculate an official SOS registration fee, and produce printable records from one browser side panel.


==================================================
PERMISSION JUSTIFICATIONS  (one box per permission)
==================================================

sidePanel —
The entire user interface is a Chrome side panel; this permission opens and renders it.

storage —
Saves preferences and bounded device-local customer/report history (up to 30 days / 50 records) so an authorized dealership user can reopen, re-screen, print, or download a prior record.

unlimitedStorage —
Stores the downloaded OFAC SDN sanctions dataset and bounded device-local report history, including captured state-site evidence images. SOS credentials and authentication tokens are not stored in History.

alarms —
Schedules a daily attempt to refresh the local OFAC sanctions list and maintain the 30-day history limit.

tabs —
Creates an inactive, extension-owned public Michigan SOS calculator tab only after the user clicks Calculate SOS fee; closes it after a verified result; and foregrounds the same prefilled tab only if the user explicitly clicks Finish on Michigan SOS after automatic attempts fail.

Host permission https://sanctionslistservice.ofac.treas.gov/ —
Downloads the official U.S. Treasury OFAC SDN sanctions list used for on-device screening.

Host permission https://wc2h-sls-prod-public-published.s3.us-gov-west-1.amazonaws.com/ —
Allows the official Treasury Sanctions List Service download to follow its signed redirect to Treasury's dedicated AWS GovCloud file host. Only the public OFAC list is downloaded.

Host permission https://compliance-central-api.fly.dev/ —
The dealer's backend endpoint that performs the Repeat Offender and Title/Lien checks against the Michigan Department of State portal. It also relays the optional phone license-scan: the scanned fields are end-to-end encrypted on the phone, so the backend only passes along an encrypted, single-use package it cannot read (deleted on pickup; the license image is never transmitted).

The hosting service receives ordinary network request metadata. Compliance Central uses request IP addresses transiently in memory for rate limiting and abuse prevention; the app does not write them to a database or application log.

Host permission https://dsvsesvc.sos.state.mi.us/ —
Uses the public Michigan SOS registration-fee calculator after an explicit user action, verifies and applies the completed local choices, and returns the official fee plus a session-only image of the actual result page. The extension does not access SOS credentials or cookies.

Host permission https://vpic.nhtsa.dot.gov/ —
Sends a VIN or partial VIN directly to NHTSA's public vPIC decoder only after the user clicks Decode, then uses supported vehicle details to fill local fee fields for review. The raw VIN is not retained in the decoder response.

Are you using remote code?  →  NO
(All code is bundled in the package; nothing is fetched and executed remotely.)


==================================================
DATA USAGE  (the checkbox form)
==================================================
"Does this item collect or use user data?"  →  YES

Data types to mark as COLLECTED:
  • Personally identifiable information  (name, date of birth, government ID — Michigan DLN/PID)
  • Website content  (Michigan Repeat Offender and title/lien responses and screenshots saved in bounded device-local History; SOS fee result-page captures remain session-only)
  • Location  (the hosting service receives the request IP address; Compliance Central uses it transiently in memory for rate limiting and abuse prevention, and does not write it to a database or application log)

Data types to leave UNCHECKED:
  • Web history: NO
  • Financial, health, authentication, personal communications, user activity, etc.: NO
  (The trade-in VIN is sent only for the optional title check — note under "other" if asked. Customer fields, VINs, full results, and portal screenshots are retained in device-local History for up to 30 days / 50 records so dealership staff can reopen and reproduce a record. User-requested PDF or CSV downloads remain in the user's chosen location.)

Check ALL THREE required certifications (they are all true for this extension):
  [x] I do not sell or transfer user data to third parties, outside of the approved use cases.
  [x] I do not use or transfer user data for purposes that are unrelated to my item's single purpose.
  [x] I do not use or transfer user data to determine creditworthiness or for lending purposes.

Privacy policy URL:
https://techsavvyjoe.github.io/compliance-central/


==================================================
LIMITED USE DISCLOSURE
==================================================
Compliance Central's use of information received from Google APIs adheres to the Chrome Web Store User Data Policy, including the Limited Use requirements.

Information is used only to provide the extension's user-requested compliance checks, phone-to-computer transfer, local preferences, and device-local customer/report records. It is not used for advertising, market research, credit scoring, or unrelated purposes. Human access is limited to the authorized user of the local browser profile, a user's explicit support request, security investigation, or legal requirement.
